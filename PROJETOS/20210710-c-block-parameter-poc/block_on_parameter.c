#include<stdio.h>

// NOTE: Does not work

int sum(int x, int y) {
    return x + y;
}

int main() {
    int test = {
        int n = 2;
        return n;
    };
    int res = sum(
        {int n = 2;return n;},
        {int n = 2; return n;}
    );
    return 0;
}
