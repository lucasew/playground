version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: db
        out: db
        sql_package: "pgx/v5"
        emit_db_tags: true
        emit_prepared_queries: true
        emit_json_tags: true
